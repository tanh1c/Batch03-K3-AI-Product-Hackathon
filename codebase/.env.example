# Optional: without a key, the Tutor still works in transparent demo mode.
OPENAI_API_KEY=

# Current balanced default; change this if your OpenAI project uses another model.
OPENAI_MODEL=gpt-5.6-terra

# Optional local port.
PORT=3000
